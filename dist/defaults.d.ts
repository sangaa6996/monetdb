declare const defaults: {
    host: string;
    port: string | number;
    username: string;
    password: string;
    database: string;
    autoCommit: boolean;
    replySize: number;
};
export default defaults;
